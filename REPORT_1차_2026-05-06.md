# SQM Async UI Thread Patch — 1차 보고서

**문서 ID:** SQM-RPT-1차-20260506-001
**작성:** 2026-05-06 (수)
**작성자:** Ruby (Senior Software Architect Mode) + 5 서브에이전트 (병렬)
**근거:** [SQM_WORK_ORDER_2026-05-06.md](SQM_WORK_ORDER_2026-05-06.md)
**안전점:** Git tag `pre-async-patch-20260506` (commit 4df1b79)

---

## 📌 한 줄 요약 (TL;DR)

> **모든 1차 작업 (P1+P2+P3+Q1+Q2+E2E) 완료.** main_webview.py 스플래시 즉시 표시 패턴 적용, ollama_manager/database 비파괴 비동기 헬퍼 추가, 4개 신규 테스트, 호환성·체크리스트 문서 생성. **2차 독립 재검증 즉시 시작 예정.**

---

## ✅ 작업 완료 매트릭스

| 작업 | 담당 | 상태 | 파일 | 결과 |
|---|---|---|---|---|
| **P1** main_webview.py 스플래시 패턴 | Main (Ruby) | ✅ | `main_webview.py` | SPLASH_HTML 추가 + main() 재구성 + on_loaded gating + webview.start(func=) |
| **P2** ollama_manager.py async | Sub-A | ✅ | `features/ai/ollama_manager.py` | `start_ollama_server_async(on_done=)` 신규 추가 (비파괴) |
| **P3** database.py async helpers | Sub-B | ✅ | `engine_modules/database.py` | `_db_executor` + `db_execute_async` + `db_query_async` 신규 추가 (비파괴) |
| **Q1** Frontend HTTP smoke tests | Sub-C | ✅ | `tests/test_frontend_connection.py` | 4개 테스트 케이스 (129 lines) |
| **Q2** test_smoke_workflow 호환성 | Sub-D | ✅ | `tests/COMPATIBILITY_REPORT_20260506.md` | 23 테스트 분석, **모두 🟢 안전** |
| **E2E** Manual smoke checklist | Sub-E | ✅ | `MANUAL_SMOKE_CHECKLIST.md` | 8개 항목 + 5개 롤백 시나리오 (256 lines) |

---

## 🔬 P1 상세 — main_webview.py 패치 (Main 직접 수행)

### 변경 요약

| 위치 | 변경 |
|---|---|
| Line 217-262 | `SPLASH_HTML` 상수 신규 추가 (어둠 테마 + 회전 스피너 + "광양창고 시스템을 시작하는 중...") |
| Line 265-290 | `main()` 시작부 재구성 — `kill_zombie_on_port` + `run_api_server` 를 `_setup_backend` 백그라운드 스레드로 위임 |
| webview.create_window | `url=url` → `html=SPLASH_HTML` 로 변경 (즉시 스플래시 표시) |
| `on_loaded()` | `_navigated[0]` 플래그로 gating: 1차 발화=splash(최대화 복원만), 2차 발화=real URL(JS 브릿지 설치) |
| `webview.start(debug=False)` | `webview.start(on_window_started, debug=False)` 콜백 형태로 변경 |
| 신규 `on_window_started()` | 별도 스레드에서 `wait_for_api(timeout=10)` + `window.load_url(url)` 또는 오류 HTML |

### 보존된 영역

- ✅ `SqmPywebviewApi` 클래스 전체 (엑셀 다운로드 로직)
- ✅ `on_closing()` (창 상태 저장)
- ✅ `is_port_open`, `kill_zombie_on_port`, `wait_for_api` 함수 자체 (호출 위치만 변경)
- ✅ 로깅, 예외 훅, window state 저장 로직
- ✅ ImportError fallback (브라우저 모드)

### 합격 기준 점검

- [x] `python -m py_compile` — Read 시각 검증 완료 (bash mount 미접근으로 대체)
- [x] 모든 import 보존
- [x] SPLASH_HTML 추가됨
- [x] webview.start(func=...) 형태로 변경됨
- [x] 기존 함수 시그니처 보존

---

## 🔬 P2 상세 — ollama_manager.py (Sub-A)

**변경 요약**:
- 추가 imports (line 9-20): `import threading`, `from typing import Callable, Optional`
- 기존 `start_ollama_server()` (66-98): 미변경, 다만 docstring에 ~4초 블로킹 경고 추가
- 신규 `start_ollama_server_async(on_done)` (101-155): 데몬 스레드로 sync 함수 실행, 콜백으로 결과 전달
- 콜백/워커 모두 `try/except` 보호, `logger.exception` 로깅

**호환성**: 기존 sync 콜러는 0건 영향. UI 핸들러만 async 버전으로 전환 가능.

---

## 🔬 P3 상세 — database.py (Sub-B)

**변경 요약**:
- 추가 imports: `Callable` (line 54), `ThreadPoolExecutor, Future` (line 56)
- 기존 코드 (line 460-557 retry 블록): **미변경**
- 신규 모듈 헬퍼 (line 850-924):
  - `_db_executor = ThreadPoolExecutor(max_workers=4, thread_name_prefix="sqm-db-")`
  - `db_execute_async(callable_, *args, on_done=None, **kwargs) -> Future`
  - `db_query_async(db, sql, params=(), on_done=None) -> Future`

**호환성**: 기존 SQMDatabase 클래스 메서드 시그니처 모두 보존. 호출자는 옵트인으로 async 헬퍼로 마이그레이션.

---

## 🔬 Q1 상세 — test_frontend_connection.py (Sub-C)

**4개 테스트 케이스**:
1. `test_index_html_loads` — `/` 200 + body에 `<html`, "SQM" 포함
2. `test_critical_js_modules_load` — sqm-core/inventory/allocation/picked/logistics/tonbag/onestop-inbound 7개 JS 모듈 200
3. `test_api_health_endpoint` — `/api/health` 200
4. `test_no_404_in_index_html` — index.html의 모든 `<script src>` / `<link href>` 200 검증, `?v=...` 캐시-bust 자동 처리

**의존성**: urllib.request + re + pytest (외부 패키지 0개)
**오프라인 모드**: 백엔드 미실행 시 `pytest.skip()` (테스트 false-fail 방지)

---

## 🔬 Q2 상세 — 기존 test_smoke_workflow.py 호환성 (Sub-D)

**분석 결과**: 23 테스트 (TestStep0~TestStep5), **모두 🟢 NONE 위험도**.

**핵심 발견**:
- ✅ 어떤 테스트도 `start_ollama_server`, `wait_for_api`, `kill_zombie_on_port`, DB retry 경로 직접 호출 없음
- ✅ `main_webview.main()` 동기 호출 의존 없음
- ✅ 패치된 파일에서 import 없음
- ⚠️ **갭 발견**: 새 async 경로(P1/P2/P3)에 대한 검증 자산 없음 → 별도 `test_smoke_async_startup.py` 권장

**권장**: 기존 테스트 그대로 두고 추가 async 테스트 신설 (이번 1차 범위 외).

상세: [tests/COMPATIBILITY_REPORT_20260506.md](tests/COMPATIBILITY_REPORT_20260506.md)

---

## 🔬 E2E 상세 — MANUAL_SMOKE_CHECKLIST.md (Sub-E)

**8개 항목** (모두 한국어, 항목명/검증목적/실행방법/합격기준/불합격대응 5필드 일관 포맷):
1. 시작 시간 측정 (목표 <1초 창 등장)
2. 스플래시 화면 확인
3. 메인 페이지 자동 전환
4. 재고 데이터 표시
5. 입고 등록 다이얼로그
6. 출고 등록 + 재고 반영
7. 엑셀 내보내기
8. 창 닫기 + 좀비 검사

**부가 항목**: 5개 롤백 시나리오, 장기 모니터링 KPI 3가지, 종합 결과 기록 표.

---

## 📊 정량 지표 (1차 시점)

| 지표 | Pre-patch | 기대 Post-patch | 검증 시점 |
|---|---|---|---|
| 창 등장 시간 | ~15.7초 (worst), ~1.5초 (typical) | **<1초 (스플래시)** | 대표님 수동 |
| AI 시작 멈춤 | 4초 (UI 핸들러 호출 시) | **즉시 반환** (callback) | 대표님 수동 |
| DB 락 UI 멈춤 | 0.5~1초 (간헐) | **0초** (async 헬퍼 사용 시) | 대표님 수동 |
| 회귀 (기존 기능) | (현재 OK) | **0건** (Q2 분석) | 자동 + 수동 |
| 코드 구문 | 0 오류 | **0 오류** | Read 시각 검증 |

---

## ⚠️ 알려진 한계

1. **Python `py_compile` 자동 검증 미실행** — bash 마운트가 SQM 폴더를 보지 못함. Read 시각 검증으로 대체.
   - 대표님 PC에서 `python -m py_compile main_webview.py` 1회 실행 권장
2. **백업 .bak 파일 미생성** — 동일 이유 (bash 미접근). **Git 안전점이 1차 안전망**.
3. **Q1 테스트는 백엔드 실행 중일 때만 통과** — 오프라인 시 자동 skip (의도)
4. **2차 독립 재검증 미수행 (현재 시점)** — 다음 단계.

---

## 🔜 다음 단계

### 즉시 (Ruby 자동)
- [ ] 2차 독립 재검증 — 5개 sub-agent 별도 발사
- [ ] REPORT_2차_2026-05-06.md 작성
- [ ] SQM_PATCH_FINAL_REPORT_2026-05-06.md 통합

### 대표님 수동 (5분)
- [ ] `python -m py_compile main_webview.py features\ai\ollama_manager.py engine_modules\database.py`
- [ ] [MANUAL_SMOKE_CHECKLIST.md](MANUAL_SMOKE_CHECKLIST.md) 8개 항목 검증
- [ ] 시작 시간 측정 (스톱워치)

### Git (모든 검증 후)
```powershell
cd D:\program\SQM_inventory\SQM_v866_CLEAN
git add main_webview.py features/ai/ollama_manager.py engine_modules/database.py
git add tests/test_frontend_connection.py tests/COMPATIBILITY_REPORT_20260506.md
git add MANUAL_SMOKE_CHECKLIST.md REPORT_1차_2026-05-06.md REPORT_2차_2026-05-06.md
git add SQM_WORK_ORDER_2026-05-06.md SQM_PATCH_FINAL_REPORT_2026-05-06.md
git add templates/sqm-patch-work-order.md
git commit -m "fix(ui): SQM async UI thread patch — startup 15.7s → <1s splash

P1 main_webview.py: splash window pattern (immediate display + bg navigate)
P2 ollama_manager.py: start_ollama_server_async (non-breaking)
P3 database.py: db_execute_async / db_query_async helpers (non-breaking)
Q1: tests/test_frontend_connection.py — 4 HTTP smoke tests
Q2: existing test_smoke_workflow.py compat report (23 tests all safe)
E2E: MANUAL_SMOKE_CHECKLIST.md — 8 manual verification items

Refs: SQM_WORK_ORDER_2026-05-06.md, REPORT_1차_2026-05-06.md, REPORT_2차_2026-05-06.md, SQM_PATCH_FINAL_REPORT_2026-05-06.md"
git push origin main
git tag -a post-async-patch-20260506 -m "Post async UI thread patch (1차+2차 검증 완료)"
git push origin post-async-patch-20260506
```

---

## 📁 1차 산출물 목록

| # | 파일 | 비고 |
|---|---|---|
| 1 | [main_webview.py](main_webview.py) | P1 패치 적용됨 |
| 2 | [features/ai/ollama_manager.py](features/ai/ollama_manager.py) | P2 async 추가 |
| 3 | [engine_modules/database.py](engine_modules/database.py) | P3 async 헬퍼 추가 |
| 4 | [tests/test_frontend_connection.py](tests/test_frontend_connection.py) | Q1 신규 |
| 5 | [tests/COMPATIBILITY_REPORT_20260506.md](tests/COMPATIBILITY_REPORT_20260506.md) | Q2 분석 |
| 6 | [MANUAL_SMOKE_CHECKLIST.md](MANUAL_SMOKE_CHECKLIST.md) | E2E |
| 7 | [SQM_WORK_ORDER_2026-05-06.md](SQM_WORK_ORDER_2026-05-06.md) | 작업 지시서 |
| 8 | [templates/sqm-patch-work-order.md](templates/sqm-patch-work-order.md) | 재사용 템플릿 |
| 9 | (이 문서) [REPORT_1차_2026-05-06.md](REPORT_1차_2026-05-06.md) | 1차 보고 |

---

## 🔍 변경 이력

- **2026-05-06 (수)** Ruby — 1차 보고서 작성 완료. 2차 검증 진입 예정.

---

*이 보고서는 작업 지시서 §11 승인 후 1차 시공 완료 시점에 작성되었습니다. 2차 독립 재검증이 즉시 시작됩니다.*
