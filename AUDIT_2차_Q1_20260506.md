# 2차 Audit Report — Q1 Frontend Connection Tests
**File**: `tests/test_frontend_connection.py`
**Date**: 2026-05-06
**Auditor**: 2nd-pass independent review

## Executive Verdict
🟢 **PASS** — All requirements met; minor enhancement opportunities only.

## Verified Items
- 4 test functions present with correct names and docstrings.
- `_get()` helper cleanly separates HTTP errors (returned with code) from connection failures (re-raised as `ConnectionError`). **404/500 will FAIL the assertion, not skip** — correctly handled.
- All `urlopen` calls use `timeout=2` (via `TIMEOUT` constant).
- Connection failures (`URLError`, `ConnectionRefusedError`, `OSError`) → `pytest.skip("backend not running")` in every test.
- Stdlib only (`re`, `urllib.request`, `urllib.error`) + `pytest`. No external deps.
- Regex handles **both** single and double quotes: `["\']([^"\']+)["\']`.
- CDN (`http://`, `https://`, `//`) and `data:` URIs correctly skipped.
- Leading `/` stripped via `lstrip("/")` before URL join.
- `?v=...` cache-bust suffix is implicitly preserved in the URL — backend's StaticFiles ignores query string, so this works (verified: `js/sqm-core.js?v=20260505a` will be requested as-is and served).
- `checked > 0` guard prevents silent pass when no assets parsed.
- Coverage: all 7 `<script src>` modules from `index.html` (lines 366–372) are in `CRITICAL_JS_MODULES`. Match is exact.
- Tests are independent — no shared state, no fixtures, no global mutation.
- Syntax: clean, no issues.

## Issues Found
**None of severity High.**

### Low — minor improvements
1. `test_critical_js_modules_load` probes the first URL twice (once as the connectivity probe, then again in the loop). Harmless but redundant.
2. `favicon.ico` (line 7 of index.html) is referenced via `<link rel="icon" href="favicon.ico">` — the regex matches `<link[^>]+href=...` so `test_no_404_in_index_html` **will** validate it. Good (no gap).
3. CSS files (`design-system.css`, `v864-layout.css`) are **only** validated by `test_no_404_in_index_html`, not by a dedicated CSS test. Acceptable since `test_no_404` covers them, but a dedicated `CRITICAL_CSS` list would be more explicit.
4. `_get` swallows `HTTPError.code` but returns `b""` body — fine for status-only checks, but if a future test wants to inspect a 404 body it would fail silently.

## Coverage Gaps
- **None for JS**: 7/7 `<script src>` modules covered by `CRITICAL_JS_MODULES`.
- `test_api_health_endpoint` checks only `/api/health`. Other startup-critical endpoints (e.g., dashboard KPI endpoints used immediately on load) are not probed — but this is outside Q1 scope.

## Recommended Fixes / Additions (optional, not blocking)
1. Remove the redundant probe in `test_critical_js_modules_load` (just let the first iteration's `ConnectionError` handler cover it).
2. Consider adding `favicon.ico` and the two CSS files to a `CRITICAL_STATIC` list for parallel structure with JS.
3. Consider asserting `Content-Type` on at least one JS response (e.g., `application/javascript`) to catch StaticFiles misconfiguration.

## Final
File is production-ready. Ship it.
