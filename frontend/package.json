{
  "name": "sqm-v864-3-frontend",
  "version": "8.6.5",
  "private": true,
  "description": "SQM Inventory v8.6.5 frontend — CSS/JS linting only (no build step).",
  "scripts": {
    "lint:css": "stylelint \"css/*.css\"",
    "lint:css:fix": "stylelint \"css/*.css\" --fix",
    "lint": "npm run lint:css"
  },
  "devDependencies": {
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  }
}
